import {createServer} from 'node:http';import {readFileSync,existsSync}from'node:fs';import{extname,join}from'node:path';import ts from '/root/.nvm/versions/node/v24.15.0/lib/node_modules/typescript/lib/typescript.js';
const mime={'.html':'text/html','.js':'text/javascript','.ts':'text/javascript','.css':'text/css'};createServer((q,r)=>{let p=q.url==='/'?'index.html':q.url.slice(1);let f=join(process.cwd(),p);if(!existsSync(f)){r.writeHead(404);return r.end('not found')}r.setHeader('content-type',mime[extname(f)]||'text/plain');let s=readFileSync(f,'utf8');if(extname(f)==='.ts')s=ts.transpileModule(s.replace("import './style.css';",''),{compilerOptions:{target:ts.ScriptTarget.ES2020,module:ts.ModuleKind.ES2020}}).outputText;r.end(s)}).listen(5173,'0.0.0.0',()=>console.log('Vite-compatible dev server: http://localhost:5173'));
